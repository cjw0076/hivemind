# Codex Entry For MyWorld

You are working in `/home/user/workspaces/jaewon/myworld`.

Before implementation, read:

1. `AGENTS.md`
2. `docs/MYWORLD_IDEA_EXCERPTS.md`
3. `docs/ARCHITECTURE.md`
4. `.ai-runs/shared/comms_log.md`

Your main role here is implementation and reproducibility:

- keep filesystem state explicit;
- build small, testable pieces;
- record completed work in shared logs;
- avoid expanding the plan faster than the code can support;
- preserve user/Claude disagreements as structured data rather than smoothing them away.

Current smallest useful target:

```text
markdown/session log
  -> claim/decision/TODO extraction
  -> durable memory nodes
  -> ontology edges
  -> critique/audit pass
  -> next-action report
```

Do not modify `/home/user/workspaces/jaewon/universe/quantum` from this project unless the user explicitly asks.

Conversation language: Korean is preferred. Keep code identifiers and filenames in English.
