# Private Data Directory

Do not commit raw exports, provider transcripts, session logs, credentials, or
personal data from this directory.

`data/*` is ignored by git. Only this README is intended to be tracked.
